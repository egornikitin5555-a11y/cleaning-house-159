# Инструкции по загрузке на GitHub и настройке GitHub Pages

## 🚀 Первоначальная загрузка на GitHub

### 1. Создание нового репозитория на GitHub

1. Зайдите на [github.com](https://github.com)
2. Нажмите зеленую кнопку "New repository" или "+" → "New repository"
3. Введите название репозитория (например: `cleaning-service-website`)
4. Выберите **Public** (для бесплатного GitHub Pages)
5. Отметьте "Add a README file"
6. Нажмите "Create repository"

### 2. Клонирование и загрузка файлов

Откройте терминал в папке `github_deployment` и выполните команды:

```bash
# Перейдите в папку с файлами
cd github_deployment

# Инициализируйте git репозиторий
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: Cleaning service website with hero video fixes"

# Добавьте URL вашего GitHub репозитория (замените YOUR_USERNAME и YOUR_REPO_NAME)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# Загрузите файлы на GitHub
git branch -M main
git push -u origin main
```

## 🔧 Настройка GitHub Pages

### Способ 1: Через веб-интерфейс GitHub (Рекомендуется)

1. Откройте ваш репозиторий на GitHub
2. Перейдите в раздел **Settings** (вверху репозитория)
3. В левом меню найдите **Pages**
4. В разделе "Source" выберите:
   - **Deploy from**: Branch
   - **Branch**: main
   - **Folder**: / (root)
5. Нажмите **Save**

### Способ 2: Через GitHub Actions (для автоматической публикации)

Создайте файл `.github/workflows/deploy.yml` в корне репозитория:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Pages
      uses: actions/configure-pages@v2
      
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v1
      with:
        path: './'
```

## 🌐 Доступ к сайту

После настройки GitHub Pages ваш сайт будет доступен по адресу:
`https://YOUR_USERNAME.github.io/YOUR_REPO_NAME`

Если вы назвали репозиторий `YOUR_USERNAME.github.io`, то сайт будет доступен по:
`https://YOUR_USERNAME.github.io`

## 🔄 Обновление сайта

После внесения изменений выполните:

```bash
# Добавьте изменения
git add .

# Сделайте коммит с описанием изменений
git commit -m "Update: [опишите что изменили]"

# Загрузите на GitHub
git push origin main
```

GitHub Pages автоматически обновится через несколько минут.

## ✅ Проверка настроек

1. Убедитесь, что файл `index.html` находится в корне репозитория
2. Проверьте, что все ссылки на изображения в `styles.css` и `index.html` корректные
3. Убедитесь, что видео `hero_video.mp4` находится в папке `imgs/`

## 🎯 Структура файлов на GitHub

```
YOUR_REPO_NAME/
├── index.html
├── styles.css
├── README.md
├── FIXES_REPORT.md
├── comparison.html
└── imgs/
    ├── hero_video.mp4
    ├── logo.png
    └── [другие изображения]
```

## 📋 Команды для быстрого копирования

```bash
# Если репозиторий уже существует
git clone https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
cd YOUR_REPO_NAME

# Скопируйте файлы из github_deployment/ в корень репозитория
# Затем:
git add .
git commit -m "Initial commit: Website with hero video fixes"
git push origin main
```

## 🔍 Устранение проблем

### Проблема: Сайт не загружается
- Проверьте настройки GitHub Pages в Settings → Pages
- Убедитесь, что `index.html` в корне репозитория
- Подождите 5-10 минут после настройки

### Проблема: Видео не воспроизводится
- Убедитесь, что `hero_video.mp4` в папке `imgs/`
- Проверьте формат видео (должен быть MP4)
- Проверьте размер файла (не более 10MB)

### Проблема: Изображения не отображаются
- Проверьте пути к изображениям в HTML/CSS
- Убедитесь, что изображения загружены в `imgs/`
- Проверьте регистр букв в названиях файлов

## 📱 Тестирование

После развертывания протестируйте сайт на:
- Desktop браузерах (Chrome, Firefox, Safari)
- Мобильных устройствах
- Планшетах
- Различных разрешениях экрана

Убедитесь, что hero секция с видео отображается корректно на всех устройствах!